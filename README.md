This is a custom class that contains additional mathematical methods:

* Greatest Common Factor:
	The below static method returns the greatest common factor of two integers passed to it. It takes two integers as an arguement:
	RMath.gcd(int x, int y) is the method signature
